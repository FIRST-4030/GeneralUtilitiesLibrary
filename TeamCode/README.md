# TeamCode

This module contains the robot-specific FTC OpModes and tests for the GeneralUtilities project.

Reusable classes have been moved into the `GeneralUtilities` Android library module so they can be
published and consumed by other FTC projects as a Gradle dependency.

The top-level `README.md` remains unchanged.
